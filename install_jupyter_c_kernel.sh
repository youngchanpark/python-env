
# https://github.com/brendan-rius/jupyter-c-kernel

# Install the c kernel package
python3 -m pip install jupyter-c-kernel 

# Run below command to add the kernel information under the `kernels` directory 
install_c_kernel

# `c` directory should be under the below path
ls /usr/local/share/jupyter/kernels/



